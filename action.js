// removing elements:

// pop method:
const number=[1,2,3,4,5]
number.pop()

console.log(number);

// shift method:
const items=["apple","orange","lemon"]
items.shift()

console.log(items);

// splice method:
const twoWhelers=["hero","ktm","duke"]
twoWhelers.splice(1,1,"tvs")

console.log(twoWhelers);